# Core LangChain framework
langchain
langchain-community
langchain-core
langchain-text-splitters

# Vector database
chromadb
langchain-chroma

# Local embeddings (no API costs, no rate limits)
# Note: sentence-transformers 3.0.1 is pinned to avoid PyTorch compatibility issues
sentence-transformers==3.0.1
langchain-huggingface

# LLM integration (for query/generation phase)
langchain-google-genai

# Local LLM for evaluation (unlimited usage)
langchain-ollama

# Groq API for fast evaluation (14,400 requests/day free)
langchain-groq

# Frontend
streamlit

# Document processing
pypdf

# Environment management
python-dotenv

# Progress bars for better UX
tqdm

# Hybrid Search - BM25 retriever
rank-bm25

# Scientific computing (NumPy <2.0 for sentence-transformers compatibility)
numpy<2.0
scikit-learn

# RAG Evaluation
ragas
datasets
pandas